version: 0.2

env:
  # Remove the parameter-store section since we'll use direct credentials

phases:
  install:
    runtime-versions:
      python: 3.11
  pre_build:
    commands:
      - echo "Installing dependencies..."
      - pip install -r attempt-1/simple-python-app/requirements.txt
  build:
    commands:
      - echo "Running tests..."
      - cd attempt-1/simple-python-app/
      - echo "Building Docker image..."
      - docker login -u devops8297 -p 14n31a0370@MEC docker.io
      - docker build -t docker.io/devops8297/simple-python-flask-app:latest .
      - docker push docker.io/devops8297/simple-python-flask-app:latest
  post_build:
    commands:
      - echo "Build completed successfully!"
artifacts:
  files:
    - '**/*'
  base-directory: ../simple-python-app
